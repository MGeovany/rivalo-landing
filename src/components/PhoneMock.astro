<aside class="device-wrap" aria-label="Rivalo app preview">
  <div class="device">
    <div class="device-notch"></div>
    <div class="screen">
      <!-- Status bar -->
      <div class="status-bar">
        <span class="status-time">9:41</span>
        <div class="status-icons">
          <span></span><span></span><span></span>
        </div>
      </div>

      <!-- App header -->
      <div class="app-header">
        <img src="/images/wordmark.png" alt="" class="app-wordmark" width="72" height="20" />
        <div class="avatar"></div>
      </div>

      <!-- Period pills -->
      <div class="pills">
        <span class="pill active">Week</span>
        <span class="pill">Month</span>
        <span class="pill">Season</span>
      </div>

      <!-- Hero metric card -->
      <div class="hero-card">
        <span class="hero-label">THIS WEEK</span>
        <div class="hero-metric">18.4<span class="hero-unit">km</span></div>
        <div class="hero-sub">
          <span class="delta positive">+2.1 km</span>
          <span>vs last week</span>
        </div>
      </div>

      <!-- Stat grid -->
      <div class="stat-grid">
        <div class="stat-card">
          <div class="stat-icon gold">⚡</div>
          <div class="stat-value">34</div>
          <div class="stat-label">SPRINTS</div>
        </div>
        <div class="stat-card">
          <div class="stat-icon blue">♥</div>
          <div class="stat-value">152</div>
          <div class="stat-label">AVG BPM</div>
        </div>
        <div class="stat-card">
          <div class="stat-icon orange">★</div>
          <div class="stat-value">87</div>
          <div class="stat-label">RATING</div>
        </div>
        <div class="stat-card">
          <div class="stat-icon green">🔥</div>
          <div class="stat-value">3</div>
          <div class="stat-label">MATCHES</div>
        </div>
      </div>

      <!-- Streak chip -->
      <div class="streak-chip">
        <span class="streak-dot"></span>
        2-week streak active
      </div>
    </div>
  </div>
  <div class="device-glow" aria-hidden="true"></div>
</aside>

<style>
  .device-wrap {
    position: relative;
    display: flex;
    justify-content: center;
  }

  .device-glow {
    position: absolute;
    inset: 10% 5% -5%;
    background: radial-gradient(ellipse at 50% 60%, rgba(255, 90, 0, 0.18), transparent 65%);
    pointer-events: none;
    z-index: 0;
  }

  .device {
    position: relative;
    z-index: 1;
    width: min(100%, 300px);
    border: 1.5px solid var(--line-strong);
    border-radius: 44px;
    padding: 12px;
    background: linear-gradient(160deg, rgba(255,255,255,0.08), rgba(255,255,255,0.02));
    box-shadow:
      0 40px 80px rgba(0, 0, 0, 0.55),
      inset 0 1px rgba(255,255,255,0.1);
  }

  .device-notch {
    width: 90px;
    height: 24px;
    margin: 0 auto 8px;
    border-radius: 0 0 16px 16px;
    background: #000;
  }

  .screen {
    border-radius: 34px;
    padding: 0 16px 20px;
    background: var(--bg);
    overflow: hidden;
  }

  .status-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 8px 4px 12px;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    font-weight: 700;
    color: var(--text);
  }

  .status-icons {
    display: flex;
    gap: 4px;
  }

  .status-icons span {
    width: 14px;
    height: 8px;
    border-radius: 2px;
    background: var(--text);
    opacity: 0.7;
  }

  .app-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 14px;
  }

  .app-wordmark {
    height: 18px;
    width: auto;
    opacity: 0.95;
  }

  .avatar {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--accent), var(--accent-bright));
    border: 2px solid rgba(255, 90, 0, 0.4);
  }

  .pills {
    display: flex;
    gap: 6px;
    margin-bottom: 14px;
  }

  .pill {
    padding: 5px 12px;
    border-radius: 999px;
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    background: var(--surface);
    color: var(--muted-soft);
    border: 1px solid var(--line);
  }

  .pill.active {
    background: var(--accent);
    color: #000;
    border-color: transparent;
  }

  .hero-card {
    padding: 18px;
    margin-bottom: 12px;
    border-radius: var(--radius-card-lg);
    background: linear-gradient(145deg, var(--hero-gradient-start), var(--surface));
    border: 1px solid rgba(255, 90, 0, 0.22);
  }

  .hero-label {
    display: block;
    margin-bottom: 6px;
    font-family: var(--font-mono);
    font-size: 0.6rem;
    font-weight: 400;
    letter-spacing: 0.12em;
    color: var(--muted);
    text-transform: uppercase;
  }

  .hero-metric {
    font-family: var(--font-mono);
    font-size: 2.75rem;
    font-weight: 700;
    line-height: 1;
    background: linear-gradient(135deg, #fff, var(--accent-bright));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-unit {
    font-size: 1.1rem;
    margin-left: 4px;
    opacity: 0.7;
  }

  .hero-sub {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-top: 8px;
    font-size: 0.75rem;
    color: var(--muted);
  }

  .delta.positive { color: var(--positive); font-weight: 600; }

  .stat-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
    margin-bottom: 12px;
  }

  .stat-card {
    padding: 12px;
    border-radius: 14px;
    background: linear-gradient(160deg, rgba(36,37,38,0.9), rgba(26,26,28,0.9));
    border: 1px solid var(--line);
  }

  .stat-icon {
    display: grid;
    place-items: center;
    width: 28px;
    height: 28px;
    margin-bottom: 8px;
    border-radius: 8px;
    font-size: 0.85rem;
  }

  .stat-icon.gold { background: rgba(255, 217, 89, 0.18); }
  .stat-icon.blue { background: rgba(115, 217, 255, 0.18); }
  .stat-icon.orange { background: rgba(255, 90, 0, 0.18); }
  .stat-icon.green { background: rgba(0, 217, 115, 0.18); }

  .stat-value {
    font-family: var(--font-mono);
    font-size: 1.4rem;
    font-weight: 700;
    line-height: 1;
    color: var(--text);
  }

  .stat-label {
    margin-top: 4px;
    font-family: var(--font-mono);
    font-size: 0.55rem;
    font-weight: 400;
    letter-spacing: 0.1em;
    color: var(--muted);
    text-transform: uppercase;
  }

  .streak-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 12px;
    border-radius: 999px;
    background: var(--accent);
    color: #000;
    font-size: 0.72rem;
    font-weight: 600;
  }

  .streak-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #000;
    opacity: 0.6;
  }
</style>
